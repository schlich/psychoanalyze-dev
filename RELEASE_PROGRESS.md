# Release Progress

_This file tracks progress toward upcoming releases._

Create milestones and assign issues to track progress automatically.

The workflow runs when issues/PRs are closed or milestones are updated.

To run locally:
```bash
export GITHUB_TOKEN=your_token
python3 .github/scripts/update_progress.py schlich psychoanalyze-dev
```
